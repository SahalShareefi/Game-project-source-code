using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;
/*
[CustomEditor(typeof(city_Build_Manager))]
public class city_Build_Manager_Editor : Editor {
    public override void OnInspectorGUI()
    {
        DrawDefaultInspector();

        city_Build_Manager myScript = (city_Build_Manager)target;
        if (GUILayout.Button("save map "))
        {
            myScript.saveMap();
        }
        if (GUILayout.Button("Clear map"))
        {
            myScript.ClearMap();
        }
        if (GUILayout.Button("Load map "))
        {
            myScript.loadMap();
        }
    }

}
*/
